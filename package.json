{
  "name": "ezpatent",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^1.0.0",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "@types/node": "^9.4.0",
    "@types/puppeteer": "^1.0.0"
  },
  "scripts": {
    "start": "yarn del && tsc && node ./dist/src/app.js",
    "del": "rimraf dist/*"
  }
}